CREATE TABLE card_number_sequence (
                                      id INT AUTO_INCREMENT PRIMARY KEY,
                                      current_value INT
);

INSERT INTO card_number_sequence (current_value) VALUES (1000);